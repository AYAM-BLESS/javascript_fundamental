//done by ayamba bless
//a function is a group of reusable codes which can be called anywhere in your program

function sayBless(){
    document.write("Hi Bless");
}

function calcArea(length, width){
    let area = length * width;
    document.write(area);
}
calcArea(7, 3);